import 'package:flutter/widgets.dart';

class PokedexDesktopView extends StatelessWidget {
  const PokedexDesktopView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
